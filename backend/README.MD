To package BarConnecte.Core project : 
```
dotnet pack BarConnecte.Core/BarConnecte.Core.csproj -o ./nupkgs  
```

To build docker image : 
```
docker compose build
```

To run the application : 
```
docker compose up -d
```